language: cpp
sudo: false

git:
  submodules: true

script:
  - ./compile-cmake.sh
  
compiler:
  - clang
  - gcc
  
os:
  - linux
  - osx

deploy:
  provider: releases
  prerelease: true
  api_key: "GITHUB OAUTH TOKEN"
  file: cmake-build/iblessing
  skip_cleanup: true
  on:
    tags: true
